# mynotes
notes about learn (contains java js vue css etc.)

### git提交代码
##### git add .
##### git commit -m "注释"
##### git push -u origin master
##### git config --global credential.helper wincred 生成本地凭证，无需重复输入用户名密码
#### github 访问慢优化
##### https://www.ipaddress.com/ 获取域名解析ip，添加至本地hosts文件
